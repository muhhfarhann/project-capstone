import RefleksiModel from "./refleksi-model";

const RefleksiPresenter = {
  getData: () => {
    // Jika nanti ada API, bisa ambil dari RefleksiModel
    return RefleksiModel;
  },
};

export default RefleksiPresenter;
