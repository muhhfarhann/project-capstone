export default class RekomendasiModel {
  // Kosong untuk sekarang, bisa ditambahkan API call nantinya
  async fetchRekomendasi() {
    // Contoh rencana fetch
    // return fetch('/api/rekomendasi').then(res => res.json());
  }
}
